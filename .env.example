# Chidori web · copiá este archivo a .env.local y completá los valores.
# Son los MISMOS que usa la app de escritorio (Supabase → Settings → API).
# La anon key es pública por diseño: el control de acceso lo hace RLS.

VITE_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGc...
